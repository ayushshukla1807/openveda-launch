{
  "buildCommand": "npm install --legacy-peer-deps --force && npx turbo build --filter=web && cd apps/web && next build",
  "installCommand": "npm install --legacy-peer-deps --force",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "rootDirectory": "apps/web"
}
